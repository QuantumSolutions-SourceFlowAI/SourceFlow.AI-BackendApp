DATABASE_URL=postgresql+psycopg://sourceflow:sourceflow@localhost:5432/sourceflow
REDIS_URL=redis://localhost:6379/0
MISTRAL_API_KEY=
DEFAULT_TENANT_ID=1
# Directory where uploaded documents are staged before Celery ingestion picks
# them up. Must be a volume shared between the api and worker containers.
UPLOAD_DIR=./uploads
